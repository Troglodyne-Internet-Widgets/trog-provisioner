#!/bin/bash

# Queue a command for post_install to run after the makefile is done.  The
# fragments call it by this name, and post_install keeps the queue.  See
# scripts/post_install for the order the tasks run in, and why.
exec "$(dirname "$0")/post_install" --queue "$@"
